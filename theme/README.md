# Тема витрины — редизайн по макетам Figma

Здесь лежат **только наши файлы** темы Shopify, а не вся тема целиком.
База — Horizon (themeStoreId 3625), она живёт в Shopify; в репозиторий
копируется лишь то, что мы правим или добавляем.

- Рабочая тема: **Italian Edit — redesign (Figma)**, `gid://shopify/OnlineStoreTheme/158724227303`,
  статус UNPUBLISHED. Сделана как копия живой темы «Italian Edit — wishlist»,
  живая тема не тронута.
- Макеты: https://www.figma.com/design/VW8CX7OXDPx4TtOzITOYOl/Italian-Editor

## Что здесь

| Файл | Назначение |
|---|---|
| `assets/ie-design.css` | Токены дизайна, шапка, карточка товара, сетка каталога. Подключается из `snippets/stylesheets.liquid` последним, чтобы перекрывать Horizon. |
| `snippets/ie-heading-style.liquid` | Шрифт, кегль и цвет заголовка секции из настроек редактора: печатает @font-face и кладёт значения в CSS-переменные секции. |
| `snippets/ie-product-card.liquid` | Карточка товара витрины: картинка 2:3, название капсом, цена, бренд. |
| `sections/ie-hero.liquid` | Герой главной во всю ширину: видео (основной вариант) или изображение. |
| `sections/ie-marquee.liquid` | Тёмная бегущая строка под героем. |
| `sections/ie-product-row.liquid` | Полоса товаров из коллекции + кнопка «Shop now». |
| `sections/ie-split-banner.liquid` | Сплит-баннер MEN / WOMEN. |
| `sections/ie-slider.liquid` | Горизонтальная листалка картинок со стрелками. Стоит под заголовком «Italian Edit Blog». Без автопрокрутки и без зацикливания. |
| `assets/ie-slider.js` | Стрелки и перетаскивание мышью, для горизонтальной и вертикальной ленты (`data-axis="y"`). Вся разница между осями собрана в таблице `AXES`, логика одна. Край считаем по краям карточек, а не по `scrollLeft`: у ленты есть поля, и прилипание оставляет её в начале не на нуле, а на ширине поля. На краю стрелка прячется совсем. Свайп пальцем и трекпад — родные. |
| `sections/ie-product.liquid` | Страница товара по макету: большая картинка слева, сведения справа, под ними — лента всех фотографий. Форма «в корзину» и вишлист взяты у темы (`product-form-component`, `wishlist-button`), поэтому корзина-шторка и аналитика работают как раньше. Количества и мгновенной покупки нет — их в макете нет. |
| `assets/ie-product.js` | Выбор фотографии из ленты и переключение вариантов. Цены приходят из Liquid уже отформатированными, чтобы не тащить в браузер правила валют. Прокрутка ленты — на общем `ie-slider.js`. |
| `sections/ie-designers.liquid` | Страница дизайнеров: слева вертикальная листалка (номер + бирка бренда), справа видео. Номер серый и строка не кликается, когда товаров дизайнера на витрине нет. |
| `sections/ie-label-strip.liquid` | Полоса брендов (фирменные этикетки). На главной больше не стоит — её место занял `ie-slider`. |
| `sections/ie-feedbacks.liquid` | Отзывы бумажными карточками. |
| `sections/ie-section-heading.liquid` | Заголовок раздела отдельной секцией — чтобы подписывать чужие блоки. Стоит над лентой Instagram от приложения. |
| `sections/ie-404.liquid` | Страница 404: одно полноэкранное видео (или изображение), без надписей. |
| `sections/ie-background-music.liquid` | Фоновая дорожка страницы: ссылка на mp3 из Настройки → Файлы. Ничего не рисует. |
| `snippets/ie-sound-control.liquid` | Динамик в шапке: клик глушит и включает весь звук, ползунок — громкость. |
| `assets/ie-sound.js` | Логика звука: состояние в localStorage, применение ко всем `<audio>`/`<video>`, запуск фоновой дорожки после жеста пользователя. |
| `snippets/header-row.liquid` | Файл темы. Переключатель Shop: Women/Men перенесён в левую колонку верхнего ряда; в правую колонку добавлено управление звуком. |
| `sections/designer-carousel.liquid` | Файл темы. Добавлены настройки заголовка (шрифт, кегль, цвет) и палитра карусели: фон карточки без фото, имя на ней, затемнение соседей, точки, подпись. |
| `sections/header-group.json` | Раскладка шапки: вордмарк по центру верхнего ряда, категории — в нижнем, ширина `full-width`. |
| `templates/index.json` | Главная страница. Между сплит-баннером и полосой брендов стоит штатная секция темы `designer-carousel` с теми же настройками, что в живой теме. |
| `templates/404.json` | 404. |
| `templates/page.designers.json` | Страница `/pages/designers`. Была алфавитным указателем на `custom-liquid`; заменена нашей секцией `ie-designers`. |
| `templates/product.json` | Страница товара. Секция `main` — наша `ie-product`; «Goes well with…» и ссылки на коллекции остались штатными. |

Файл `snippets/stylesheets.liquid` в теме дополнен одной строкой — подключением
`ie-design.css`. Локальной копии не держим: правка одна и не наша по смыслу.

## Как залить изменения

Shopify CLI в проекте не настроен, у серверного приложения нет scope
`write_themes`. Файлы заливаются мутацией `themeFilesUpsert` Admin API
(через MCP-коннектор Shopify). Порядок важен: сначала секции, потом шаблоны —
шаблон не пройдёт валидацию, если секции ещё нет.

## Открытые места

- Роликов и картинок из макета нет: герой, панели MEN/WOMEN, этикетки брендов,
  кадры Instagram, бирки дизайнеров и ролик на странице дизайнеров стоят
  с плейсхолдерами — их нужно загрузить в редакторе темы.
- Правый блок шапки — иконки темы, а в макете текст SEARCH / SIGN IN / CART.
- Секция `ie-instagram` больше не стоит на главной: её место занял блок
  приложения Square: Instagram Feed. Файл секции оставлен — она доступна
  в «Add section», если понадобится вернуться к своей вёрстке.
- Блок приложения сам по себе пустой: ему нужен **app embed** «Instagram Feed
  Gallery», он грузит скрипт приложения. Shopify включает embed только в
  опубликованной теме на момент установки приложения, а наша копия сделана
  раньше — поэтому в неё пришлось добавить его руками, в `config/settings_data.json`:

  ```json
  "blocks": {
    "15521780835499905587": {
      "type": "shopify://apps/square-instagram-feed/blocks/app-embed/8c9f3786-8bb5-488d-a397-b1285ed978df",
      "disabled": false,
      "settings": {}
    }
  }
  ```

  То же самое делается тумблером в редакторе: Настройки → App embeds.
  Если после этого фид всё равно пустой — в приложении не подключён аккаунт
  Instagram (ссылка App Configuration в панели блока).
- Карточки листалки блога пока не кликаются: ссылки пустые, потому что
  страницы блога ещё не созданы. Секция рендерит `<a>` только при заданной
  ссылке — так на витрине нет ссылок в никуда.
- Заголовок над лентой рисует не приложение, а секция `ie-section-heading`:
  у виджета свой заголовок настраивается только в самом приложении и не
  подчиняется типографике темы.

## Как проверять залитое

Витрина закрыта паролем, но **ассеты темы CDN отдаёт публично**:

```
https://italian-edit.com/cdn/shop/t/18/assets/ie-design.css?v=<любое>
```

`t/18` — номер темы редизайна (у живой темы `t/16`). Так проверяется, что
файл реально доехал до витрины, а не только до Admin API. Для проверки
разметки нужен preview-линк темы (Themes → ⋯ → Share preview) — он работает
без пароля.
- Каталог ещё не переверстан.
- На странице дизайнеров «есть товары» считается по `shop.vendors` — списку
  брендов, у которых на витрине есть хотя бы один товар. В админке брендов
  четыре (Gucci, Miu Miu, Prada, The Row), но на витрине виден только
  Miu Miu: по остальным `/collections/vendors?q=…` отдаёт 404, а
  `/collections/gucci` открывается пустой. То есть их товары не выведены
  в канал Online Store или не попадают в рынок. Пока это так, номера у них
  серые — и это правда, а не ошибка вёрстки.
- Дизайнеры заведены блоками секции, а у блоков потолок 50 штук. Если список
  должен быть длиннее, его придётся строить не блоками, а коллекциями
  (метаполе-флаг + `collection.image`) или метаобъектами.
- Коллекция `new` пустая: метку «новинка» проставляет фоновая задача. Пока
  полоса товаров на главной подставляет товары витрины целиком — выключается
  галкой в настройках секции.
- Переключатель стран и валют Shopify Markets убран из шапки (`show_country`,
  `show_language` = false): на его месте стоит управление звуком. Рынок теперь
  определяется только геолокацией, вручную покупатель его не сменит.
- Звук не проверен вживую: редактор темы перехватывает клики в превью, а сама
  витрина закрыта паролем. Нужен прогон на storefront.
